<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.cloud.mapper.PurchaseMapper">

    <!-- 구매 내역 저장 -->
    <insert id="insertPurchase">
        INSERT INTO purchase (USER_ID, RECIPE_CODE, PURCHASE_PRICE)
        VALUES (#{USER_ID}, #{RECIPE_CODE}, #{PURCHASE_PRICE})
    </insert>

    <!-- 내 구매 목록 조회 (최신순) -->
    <select id="getPurchaseListByUserId" resultType="org.cloud.dto.Purchase">
        SELECT * FROM purchase 
        WHERE USER_ID = #{USER_ID}
        ORDER BY PURCHASE_DATE DESC
    </select>

    <!-- 구매 여부 확인 -->
    <select id="checkPurchaseExist" resultType="int">
        SELECT COUNT(*) FROM purchase 
        WHERE USER_ID = #{USER_ID} AND RECIPE_CODE = #{RECIPE_CODE}
    </select>

</mapper>