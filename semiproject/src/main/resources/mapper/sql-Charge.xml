<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.cloud.mapper.ChargeMapper">

    <insert id="insertCharge">
        INSERT INTO Charge (USER_ID, AMOUNT, CHARGE_METHOD)
        VALUES (#{USER_ID}, #{AMOUNT}, #{CHARGE_METHOD})
    </insert>

    <select id="getChargeListByUserId" resultType="org.cloud.dto.Charge">
        SELECT * FROM Charge 
        WHERE USER_ID = #{USER_ID}
        ORDER BY CHARGE_DATE DESC
    </select>

    <delete id="deleteCharge">
        DELETE FROM Charge WHERE CHARGE_ID = #{CHARGE_ID}
    </delete>

</mapper>