<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.cloud.mapper.ReviewImageMapper">

    <insert id="insertReviewImage">
        INSERT INTO review_image (review_id, image_url)
        VALUES (#{reviewId}, #{imageUrl})
    </insert>

    <select id="getImagesByReviewId" resultType="org.cloud.dto.ReviewImage">
        SELECT image_id, review_id, image_url
        FROM review_image
        WHERE review_id = #{reviewId}
    </select>

    <select id="getImagesByRecipeCode" resultType="org.cloud.dto.ReviewImage">
        SELECT ri.image_id, ri.review_id, ri.image_url
        FROM review_image ri
        INNER JOIN review r ON ri.review_id = r.review_id
        WHERE r.recipe_code = #{recipeCode}
        ORDER BY ri.image_id ASC
    </select>

</mapper>
